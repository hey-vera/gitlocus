# SPDX-License-Identifier: Apache-2.0
[package]
name = "repo-conformance"
description = "Tests that this repository's documentation says what this repository does."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
# Nothing here is a library anyone would depend on. It is a test suite whose
# subject happens to be the repository rather than a crate.
publish = false

[lib]
path = "src/lib.rs"

[lints]
workspace = true
