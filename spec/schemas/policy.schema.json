{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gitlocus.dev/schemas/v0/policy.schema.json",
  "title": "Policy",
  "description": "A repository's own contribution rules, versioned in the repository it governs.",
  "type": "object",
  "required": [
    "version",
    "rules"
  ],
  "properties": {
    "version": {
      "const": 0
    },
    "rules": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name",
          "when",
          "require"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "when": {
            "type": "object",
            "required": [
              "paths"
            ],
            "properties": {
              "paths": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "string"
                }
              }
            },
            "additionalProperties": false
          },
          "require": {
            "type": "object",
            "properties": {
              "deterministic": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "default": [],
                "description": "Named checks that must be present, deterministic and passing."
              },
              "approvals": {
                "type": "integer",
                "minimum": 0,
                "default": 0
              },
              "min_tier": {
                "enum": [
                  "unknown",
                  "vouched",
                  "contributor",
                  "maintainer"
                ],
                "default": "unknown"
              },
              "signed_by": {
                "type": "object",
                "description": "Checks that must carry a verified signature, and from whom. Key is a requirement name; value is a glob matched against the verified signer identity. '*' accepts any verified signer. Declaring a signature requirement also makes the check required.",
                "additionalProperties": {
                  "type": "string",
                  "minLength": 1
                },
                "default": {}
              },
              "approvals_signed_by": {
                "type": "string",
                "minLength": 1,
                "description": "Glob an attestation's verified signer must match to count as an approval. Without it, an approval is a record whose produced_by is a string the producer chose, so an agent can approve its own work. Point it at a human identity provider."
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
